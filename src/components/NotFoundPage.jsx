import { useState } from "react"

const FeedMenu = () => {
    return(
        <main className="main-container">
            <h1>Couldn't find any content with such id</h1>
        </main>
    )
}

export default FeedMenu